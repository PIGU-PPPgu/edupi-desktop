[package]
name = "nomi-types"
description = "Minimal EduPi adapter for NomiFun computer-use result types"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
