[package]
name = "nomi-tools"
description = "Minimal EduPi adapter for the NomiFun ComputerTool trait"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
async-trait = "0.1"
serde_json = "1"
nomi-protocol = { path = "../nomi-protocol" }
nomi-types = { path = "../nomi-types" }
