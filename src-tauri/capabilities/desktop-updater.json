{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "Allow the packaged local EduPi UI to install signed EduPi desktop releases.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
