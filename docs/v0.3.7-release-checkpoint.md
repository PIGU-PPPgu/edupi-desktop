# v0.3.7 release checkpoint

Core PR #35 is merged. Desktop integration commit: `2e26de0`.
Pinned Core: `554b50e252e45b370cb1cafdb875dab67382806e`.

Completed verification:
- Desktop suite: 921 passed, 25 skipped, one public-source path failure.
  The path was sanitized; its targeted suite subsequently passed.
- TypeScript, lint, release metadata verification passed.
- Clean `npm ci` and `desktop:prepare` passed. Packaged Core includes 2091 files.
- Rust library tests: 25 passed, zero failures.
- Production compilation retains the existing dynamic export and createRequire warnings.

Release remains unpublished pending two runtime compatibility fixes:

1. Mainline workspace-resources/generated-artifacts and other management operations
   are outside the Task 7 broker allowlist. The client currently rejects them in
   lib/edupi-core-process-client.ts; desktop/core-supervisor.cjs and the Core
   runtime protocol enforce the same restricted surface. These require a paired,
   explicitly validated protocol change with mutation serialization preserved.
2. desktop/preparation-worker.mjs invokes the legacy morning brief and calendar
   runner directly without writer admission. It cannot write under a live daemon.
   The existing preparation UI must be connected to supervised execution before
   release, preserving real model execution and requested-task behavior.

Do not publish a package based only on successful compilation. Verify these two
user workflows end to end before merging Desktop PR #71 and dispatching release.yml.
Teacher-value acceptance remains evidence_pending; runtime golden-task activation
remains separately gated. No production data or live carrier was changed.
