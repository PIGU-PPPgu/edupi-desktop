# 实际流程验收

环境：macOS、本地 Next.js 开发版、独立数据与会话目录；不是已发布安装版。测试使用虚构学生，未改真实学生档案。模型使用已有 GLM-5.2，配置副本仅存本机。

## 提醒续聊

- 创建到期任务 `teacher-task-767144d7-8db5-4dc9-9f12-3196fb815024`，从提醒点击继续聊。
- 页面预填正确任务标题与输入位置；实际输入并发送简短测试消息。
- 模型回复“复测成功”；任务接口返回绑定会话 `01a07c97-48e0-706a-9098-c72f5a94d675`。
- 离开后从提醒返回相同会话 ID，原消息及回复可见。
- 实际停止并重启开发服务，页面重新读取后原会话和回复仍在。
- 首次验收发现会话创建后预填文字重新出现，已加一次性应用状态；复测发送后输入框为空、发送按钮禁用。
- 跨日发现未处理到期事项被撤下；改为截止日期不晚于今天时保留同一提醒身份，并补跨日回归。系统通知点击及安装版通知不在本次通过范围。

## 学生记录与网络

- 经名单导入接口创建“测试甲”“测试乙”，同属 703 班。
- 首次真实模型测试失败：会话未注册学生记录工具。配置 `/tmp` 与 Pi 会话 `/private/tmp` 的字符串比较造成误判。
- 修复为使用数据根目录真实路径；路径别名回归通过。
- 新会话 `01a07ca5-8614-758f-8e71-b7b3aa3a7ed6` 实际调用学生记录工具，持久化学习与互动各一条，来源指向该会话。
- 学生详情知识图谱显示学生、记录、知识点；页面缩放至 125%，复位至 100%，点击知识点显示关联记录。
- 页面修改学习记录，图中同步更新；展开历史看到旧内容。
- 页面删除学习记录后图中显示空态；人际网络仍保留两名参与者与一条互动事件，没有创建“好友”关系。
- 页面起始日期改为记录日期之后时为空，清除后恢复。
- 跨班同名和完整学生网络范围尚未验收完成，不能据此勾选 R12/R17 整项。

## 首次对话

- 暂存隔离历史会话，重启后以无会话环境打开 AI 对话。
- 复现没有项目选择导致加载/模型请求拒绝的问题。
- 自动读取配置工作区并复用目录验证接口登记后，页面显示工作区与 GLM-5.2。
- 实际发送“首次对话测试”，收到“就绪”，输入框清空。
- 这证明已配置模型、无历史会话的首次对话可用；从无 API 开始的完整新手教程仍需独立验收。

## 材料驱动备课与跨模块文件

- Core `f83a381`，Desktop 同次验收工作树；独立工作区提供数学 703 班课程及一份真实 Markdown 源文件。
- 实际运行 `desktop/preparation-worker.mjs`，得到四份文件；修改同一源文件，将橙色方格纸改为绿色，重新执行后仍是同一任务，旧 execution 标为 stale，新 execution 为 draft_ready。
- 第二次运行返回 `{"ok":true,"prepared":1,"skipped":0}`，模型会话 `01a07ccb-13ed-733d-9661-6aaeda895bff`。逐份读取四份输出，新教案与学案使用绿色方格纸、蓝色编号贴，材料清单正确说明系统已登记 2 人，不推断全班人数。
- 固定展开图为横排①②③④，⑤在②上方，⑥在②下方。独立按②为前面推导，①左、③右、④后、⑤上、⑥下；三组答案①③、②④、⑤⑥正确，新增判断题①②相对为错也正确。模型写出的“已逐题折叠验证”不作为证据，本次依据独立几何核对。
- 页面验收发现四个标题无文件按钮；修复统一索引后，教学产物页实际显示四份可点击文件。材料模块也显示相同四份，打开“本节教案”的路径与教学页一致，正文为新版本。旧版四份未混入最新列表。
- 预览默认隐藏 EduPi 内部元数据，标题显示“本节教案”；切换 Source 后元数据仍在。实际点击关闭按钮返回成功，1280×720 截图检查无溢出。
- 定向自动验证：`node scripts/test_requested_preparation.mjs`、`node scripts/test_generated_artifacts.mjs` 通过，覆盖四份文件、关联任务、可读路径、过期/移除后隐藏。`node --test scripts/preparation-materials.test.mjs scripts/model-output-repair.test.mjs` 六项通过，覆盖 Word 正文、范围、截断、删除档案人数与一次 JSON 修复。
- 这次用短篇固定题验证文件链路与答案，未证明所有学科内容质量；PDF/PPT 后续证据见下方，界面整套安装版未验收。

## 简报提醒与续聊

- 基于 Desktop `f2896fb` 后续改动，Core `f83a381`。在隔离工作区向 `runMorningBrief` 注入 `2026-09-08T08:00:00+08:00`，实际生成当天简报文件；不是等待真实早上调度。
- 复现投影把 UTC 文件修改时间当简报日期、ID 回退为数组下标的问题。每日简报改用文件名所属日期与日期标识，避免凌晨错日和列表重排串会话。
- 提醒中展开当天简报，点击查看事项，实际打开 `.edupi/output/daily/2026-09-08.md`。其余旧简报不作为今日新提醒。
- 点击继续聊，预填标题、简报摘录、来源文件及“我想补充”输入位置；实际发送后收到“简报续聊成功”。
- 绑定接口读取到 `document:daily:2026-09-08` → `01a07cdd-9942-71ec-9b84-e9f0ddfca733`。离开后经提醒返回同一会话；停止开发服务、重新启动、刷新后原消息与回复仍在。
- 重启后提醒接口仍只有一条简报提醒、绑定不变。系统通知及应用完全退出后的调度未验证。

## PPT 与 PDF 来源读取

- 生成两页真实 PPTX 隔离材料，课堂要求改为黄色方格纸；工作进程实际重新生成四份文件，返回 prepared=1，会话 `01a07ce8-d417-7178-9bbc-bf611d05e3ad`。教案、学案和清单均使用黄色方格纸，答案三组相对面仍正确。
- `node --test scripts/preparation-source-text.test.mjs scripts/preparation-materials.test.mjs` 五项通过，未跳过。PPT 调换实际 presentation 顺序后文本顺序正确，XML 字符正确解码；PDF 使用实际 PDF 字节，经本机 pdftotext 得到正文。
- Office 压缩目录限制抽出共享函数，原 Word 入口继续使用，原回归测试保留。没有新建第二套独立限制。
- 本机 PDF 文字提取通过，未打包 pdftotext 到 Windows；图片与图表未做视觉理解。材料清单明确说明只读了 PPT 两页文字。不能据此勾完 OCR 或安装版。
- XML 解析依赖固定到维护分支 0.8.15，依据 [上游安全修复记录](https://github.com/xmldom/xmldom/blob/master/CHANGELOG.md#0815)。新增独立目录的依赖复制/加载测试通过；这不替代安装包启动验收。

## 教学页审核联动

- 实际点击隔离备课任务的接受，复现 409：“该任务由 Today 待办审核管理”。教学页此前误用普通任务审核接口。
- 教学页按对象归属复用 Today 的同一审核请求，保留普通任务审核入口；不再提供该对象不支持的回滚与产物名称编辑。
- 复测 POST `/api/edupi/education` 返回 200，界面变为已接受，仅保留修改决定。刷新后历史显示接受时间和实际输入的审核意见。
- 从材料模块核对四份文件，均显示已确认；提醒接口中该任务的旧到期/准备完成提醒均为 withdrawn。
- 产物页收为四个真实文件按钮，去掉重复标题卡片；渲染测试覆盖文件仅出现一次、其他任务文件不混入、缺失文件禁用。

## 同名学生、转班与删除隔离

- Core 配套 `cb9cd73`；Desktop 学生身份改动与本记录同提交，仍为隔离开发环境。
- 导入接口创建 A班与 B班两名“验收张三”。页面分别显示班级，点击 B 的卡片后 URL 使用 B 的 ID。
- 页面手动保存 B 的特征，再读接口：A 特征仍为空，B 独立更新。学生对话入口预填 B 的 ID 与班级。
- 实际模型会话 `01a07d26-3597-7d0c-ac0f-552fd8c2c584` 先读取名单，再用 ID 写入 B 的学习记录和 A/B 共同互动。图中显示两个独立同名节点，互动为一条共同事件，没有生成好友边。
- 页面把 B 转入 C，ID 保持。旧学习记录保存 B班；同一会话后续新增记录保存 C班。A 另有一条独立学习记录，ID 与班级不变。
- 删除原生确认框显示了正确姓名和 C班，但自动化随后被弹窗锁住。这个页面确认操作未通过。已改为应用内确认框，取消/确定两条页面路径仍待浏览器恢复后验收。
- 单独经 DELETE 接口删除 C 的 ID，响应仅保留 A；再次读取 A 的学习记录仍为 1 条。原始 C 档案与事件保留为软删除，姓名总计数仍为 2，防止其旧姓名记忆转挂给 A。接口结果不替代页面验收。
- 新模型会话 `01a07d71-de9e-7364-a547-b573c1eff05c` 实际调用画像工具，结果为“学生已删除”；未恢复或新建档案。修复了模型把 ID 放进旧 student 参数时误报未建档的适配问题。
- Core 定向测试覆盖旧 key/ID 固定、同名不同班、来源重放、转班旧事件、删除 B 不影响 A、已删除 ID 拒写、画像与简报排除删除数据、A 已解决状态不抑制 B。已并入 `npm run test:students`。
- Desktop 渲染回归覆盖同名节点按 ID 分开、已删除同名人的裸姓名记忆不挂给剩余人、按 ID 删除结果校验；大班分页及安装版尚未验收。

## 后台 Word 与中文 OCR

- Desktop 基于 `9b8d81b` 的文档工具改动，Core `cb9cd73`；仍使用隔离开发环境。新增 `edupi_make_document`，实际生成标准 DOCX，不依赖 Python 或 Office；会话工具结果及历史恢复均登记文件。
- 后台任务 `agent_job_023fda940bf72cf8fa9d30b58c849ab9` 实际完成，产物 `f45e444b-97e9-481a-ab0c-3c00e2174787` 登记为 Word 教案。同一模型会话 `01a07d7e-530e-771f-9d07-3bc19544b69e` 修订并覆盖同一文件，再用 mammoth 读取正文。首次内容缺少展开图位置、后续出现错字，实际修订后对应问题消失；文件生成成功不等同内容一次正确。
- `node --test lib/edupi-document-tool.test.mjs` 4 项通过；真实 Core 环境下产物历史测试 4 项通过，无跳过。`npm test` 962 项中 944 通过、18 跳过、0 失败。跳过项不计入功能验收。
- 本机 Tesseract 的现有中文数据文件无法解析，第一次 OCR 结果错误，取消任务 `agent_job_795a9ba473d00fc0757ab1558104bc35` 后读取状态为 canceled，运行会话为空，没有登记产物。
- 从官方 tessdata_fast 获取中文数据到隔离目录，不改系统文件。重新经后台队列执行 `agent_job_4d0cccd0898c04605dbc274c950af5a2`，登记文本产物 `e30e6c90-bea9-438f-aa27-af011c3a4ff6`。读取文件与图片逐行一致：识别验收、周一 第2节 数学 703班、周二 第1节 数学 703班。
- OCR 依赖本机 Tesseract 与隔离修复的数据，尚未证明全新安装能直接识别。Word 可视排版、后台界面操作、安装版重启恢复仍待验证。

## 后台取消与重启

- 任务 `agent_job_91bc6ec34f19e82c7966de9b9c02775b` 执行到实际 `sleep 45` 工具调用时停止开发服务，重新启动后调用与桌面启动相同的 ensure 接口。原任务第二次执行，最终写入 recovery.txt，正文为恢复验收成功；无需手动重建任务。仅验证开发进程恢复，未验证安装版。
- 并行创建后立即取消 `agent_job_b5ce80c762c8b20bd1cd6d255a94ead4`，发现状态 canceled 但启动中的会话继续写出 canceled.txt。回归测试在修复前失败；修复为先持久化取消、会话启动后再次核对运行状态。
- 同样操作复测 `agent_job_0cabed2907d466c15bbc563cc02635f3`，状态 canceled、运行会话为空、取消文件不存在。未将最初失败测试算作通过。
- 并发测试同时暴露全局文件快照串归属：恢复任务误包含取消任务文件。后台完成收集限定当前任务目录，材料登记的并发归属仍在修复与复验，不能据原恢复测试声称产物隔离通过。
- 后续修复取代上述“修复中”：write/edit 按工具调用 ID 精确归属；原生文档使用完成事件返回路径；后台 bash 快照限定当前任务目录。双任务 `agent_job_a0eca567fddfb31b3edbf41082a6acba` 与 `agent_job_caa21e2281125fe50a76cba69ac2d946` 同时执行后各登记一份独立文件，重新读注册表会话分别为 `01a07d9d-0930-710e-aad3-6e2f62f1e185` 和 `01a07d9d-05ae-725d-bb83-c9a7f0084127`，正文分别属于甲、乙。普通聊天两个 bash 同时向共享目录写文件尚未覆盖。
- 文档、后台与真实 Core 产物定向测试合计 12 项通过，无跳过。页面仍受锁屏及先前原生确认框阻塞，未声称 UI 验收通过。

## 教学方法真实试用

- Core `66c839d` 修复教师输入标题不参与路由；独立备课此前完全未读取演进方法，现通过 Core 读取已发布且已批准的方法正文。只读方法，不伪造试用次数、课堂效果或路由记录。
- 隔离数据先冻结 `method-evaluation.json`，实际运行不加载方法的两份 baseline，再用 `try_teaching_method` 实际运行候选。首候选 `evo_mtrq9rsk_lkwh4k` 达到 3 次试用、两个会话；流程词评分训练 0.70→1.00，留出 0.60→1.00。
- 人工正文核对仍失败：训练输出误写“不相接，因此不是相对面”，留出输出混淆外边配对与 12 条棱。未晋级；经正式 review 接口拒绝，投影变为 retired，试用记录保留。确定性流程词评分不能证明数学正确或课堂效果。
- 修订候选 `evo_mtrqdl2o_1cqnuc` 补充相邻/相对面及外边配对事实，正在使用原冻结任务重新试用。原始输出和失败证据保留于隔离验收目录，未覆盖为成功结果。
- 后续结果取代“正在试用”：修订方法三次实际试用、两个会话完成，原冻结流程评分训练 0.70→1.00、留出 0.60→1.00。人工核对数学事实正确后，经接口提交明确标注的隔离测试审核，再通过模型 finalize、真实 reload，状态进入 promoted。未把测试审核冒充真实教师课堂反馈。
- 路由实测发现 `/tmp` 与 `/private/tmp` 别名造成 Core 目录比较及 Desktop 工作区门控失败。Core `9bd69d6` 比较真实目录，Desktop 三处门控复用路径规范化，保留历史 cwd。真实会话 `01a07efa-52b0-7320-b332-b159304439af`、`01a07efb-e372-70f5-88fd-9be51ecc8377` 路由记录 verified=true，无手改状态。
- 正式导入隔离课次 `method-reuse-acceptance`，同步候选后通过 preparation 接口运行，prepared=1。实际模型会话 `01a07efc-faf8-7833-85cc-7dbc2632b3e3` 生成四份文件；任务 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09` 的教案/学案写明方法 ID 和采用步骤1至4。
- 人工逐题核对练习：①③、②④、⑤⑥；②的邻面①③⑤⑥、对面④；平面不相接不等于相对，以①⑥为邻面反例；12−5=7对外边，均正确。仍是隔离材料内容核验，没有学生课堂效果证据。页面展示、后续方法更新触发重新备课尚未验收。

## 证据边界

记忆完整流程补验：真实会话 `01a07f9a-6fb5-7962-bb97-2f1072da8456` 调用 memory_write 记录偏好，页面教师偏好分类出现 `m_mts9gcpi_fexk`。手动修改到版本1、历史显示版本0、恢复旧版后版本2；新会话 `01a07fb0-66fb-7655-b0fc-bf98f2fd989f` 读到恢复后的正确内容。

页面删除后，新会话仍召回旧偏好，证明原实现只过滤投影。Core `f1e8d41` 修复召回、自动上下文、重复写入匹配及后台梦境/跨期分析；保存继续保留原始软删除条目和历史。完全重启服务后，新会话 `01a07fd4-d186-771b-bb5d-281b4cb8e54b` 实际 memory_recall 返回“没有找到相关记忆”，未恢复记录。新增默认回归覆盖这一失败链路和后台读取。

图片粘贴补验：使用隔离1像素PNG通过真实 Meta+V 粘贴，聊天输入出现附件和可用发送按钮；移除后发送重新禁用。测试后清理剪贴板恢复空文本。没有发送给不支持视觉的模型，不作为图片理解验收。移除图片按钮补充可访问名称。

Excel 名单补验：使用 `scripts/create-roster-acceptance-fixture.mjs` 生成明确测试名单，分别通过页面文件选择器上传 `.xlsx` 与 `.xls`，正确选中第二个“名单”工作表，预览1名学生和班级，再点击导入。后台重新读取分别为表格验收xlsx、表格验收xls，班级均为表格验收班，原名单保留。测试只写隔离目录，未使用真实教师名单。

本轮收尾检查：Desktop全测977项，957通过、20跳过、0失败；类型和lint通过。提供真实Core路径的手填并发、产物登记、方法注入定向15项中14通过、1旧fixture跳过。Core手填并发capture A→B测试证实A返回冲突且未批准B；对应Core版本 `67106a0`。跳过项目不计入完成。

成长来源文件补验：展开已发布方法，点击试用产物，在应用内实际读到对应四步复习流程；来源正文可读，关闭返回成长页面。方法详情默认隐藏YAML元数据，保留完整文件入口。

审核往返补验：从待确认看板接受后曾因选中任务被过滤而失去详情，已保留选中任务。修改决定时报400，Core `3ceef58` 修复已处理候选来源被移除的问题。实际页面接受→拒绝→接受全部成功，历史3条，最终已接受且保留修改决定；拒绝标签不再误报已完成，拒绝后重新审核入口不因隐藏产物而禁用。隔离任务仍为 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09`。

Word 页面补验：材料第二页打开 lesson.docx，再点预览，实际显示教学目标、课堂活动、指定展开图编号与修订后的绿色方格纸要求；关闭预览返回材料详情。仅验证应用内DOCX渲染，未用Office或安装版验收。

成长页面补验：教师专业成长随接受显示4份确认成果；能力成长显示实际已发布/已停用方法，展开可见方法正文与批准、试用、评估入口。文件来源按钮及每个折叠段仍待逐项点击，状态不作为课堂效果证明。

教师资料与教程补验：1280×720 下，教程第2步打开后缩为顶部条，手动填写按钮可点击。填写验收老师、数学、七年级、703，保存后页面和 onboarding API 均为已生效，检查继续进入第3步。新会话 `01a07f3f-5040-7f1e-863d-750e6d832f3e` 不调用工具直接正确回答四项资料，未要求重复介绍。已有 API 配置，未据此宣称零 API 首次连接通过。

大班分页补验：导入61名明确命名“分页测试01–61”的隔离名单，班级筛选后共3页，第二页25–48、第三页49–61；点击61打开其同名档案与对应ID。切换703只显示原2名学生，无空白末页；关闭档案后URL选中参数清除。主界面固定24/页，二级名单保留完整滚动访问。未向这些测试档案写学习或互动事实。

今天页简报摘要的 Markdown 标记与内部URL已清理，实际页面只显示内容名称，完整简报仍提供链接。

页面恢复后补验：课程链接 `calendarItem=method-reuse-acceptance&calendarKind=timetable&date=2026-09-09` 打开周三第3节数学703详情；抽屉内修改备注、保存后 API 重读及刷新均保留。已不存在的旧课次链接不会误选另一课次。

学生应用内删除确认补验：隔离档案 `student_e236169c-6c8e-4f12-a256-f3dd3fc532ff` 点击删除，取消后档案保留；再次确认删除，刷新后主名单与二级名单均从4变3，测试档案消失，选中URL清除，没有残留删除提示。原始记录为软删除留存。此结果取代此前页面确认受阻；不包含大班分页验收。

Core `eba2837` 追加方法版本联动：正文修订与撤回复用既有候选证据，任务ID不变、旧执行stale；真实存储/文件/节奏回归通过，模型使用确定性替身。该版本尚未通过真实模型修订重生成复验。

新增简报对象入口已通过隔离生成测试：课程、校历使用实际对象 ID 和日期；待确认、准备材料、失败任务使用实际任务 ID；产物文件检查存在后提供相对链接。真实执行候选生成产物后重新构建简报，文件链接可读取；重复运行不重复写入。Desktop 日期与对象解析测试通过，界面点击因锁屏未验收。这项记录不替代真实早上唤醒或通知送达。

上述通过项依据实际页面操作、模型回复和接口重新读取。单元测试及类型检查只作补充。未验证平台、未运行流程和外部账号依赖不计入完成；安装包按用户要求延后。
