# 实际流程验收

## 2026-09-12 Today 审核交互与写入链复核（取代旧的 Today/包 pin 结论）

- 失败复现：旧钉钉桥接 PID 持有 `.edupi/runtime/core-runtime-writer-admission-v1.sqlite`，Today 六种决定均在 Core 写入阶段失败，前端统一显示红色“暂时无法提交”。
- 修复证据：Core `5650151` 将桥接写入改为短时准入，最终 pin `92599b3cf14e2a7521ca695dee8a6992008ff76f`；`node scripts/test_dingtalk_bridge.mjs`、`test_application_runtime.mjs`、`test_dingtalk_runtime_supervisor.mjs` 通过。新包钉钉状态 ready，`lsof` 不再显示空闲进程持有 writer DB；独立 writer probe 成功。
- 隔离包 E2：最终 bundled server 使用临时数据根，读取一条真实 projected pending candidate，提交 `accept` 返回 HTTP 200 与 receipt `accepted`；重新 GET 后状态为 `accepted`、revision 增加、next cycle 为 `closed_accepted`，分组从 pending 进入 done。未写入正式教师数据。
- 页面证据：最终 `EduPi.app` 原生窗口可见；截图核对列名“待你决定 / 稍后处理 / 已记录”、列头说明、动作说明条和接受/调整/暂缓/稍后/停止提示/拒绝按钮。错误状态提供“本次没有写入”，快照过期提供“刷新待办”，普通失败提供“重试”。
- 浏览器只读复核补齐 R05/R10/R12/R17：Today 的日程项进入带对象参数的日程详情；观察记录展开后来源链接进入对应 session；班级→程天乐档案可在“知识图谱 / 人际互动网络”间切换，当前记录、列表/网络空态和来源文本均随页面更新。
- R14 交互补验：学生详情抽屉按 Escape 关闭并移除 URL 选中参数；768px 视口下 `scrollWidth === clientWidth === 768`，没有水平溢出。仅为 macOS 浏览器包验收。
- clean profile 资源补验：最终 `src-tauri/resources/edupi-core` 中允许的 7 个教育扩展由 `DefaultResourceLoader` 实际加载，返回 0 errors；退休 direct-writer 扩展明确缺席，符合当前 Core architecture ledger。
- R01/R02 页面复核：最终包材料页点击对话生成的 `展开与折叠学案.md` 后，材料详情抽屉显示状态、来源、日期、预览和“补充 / 修订”入口；本次只读打开，未改正式数据。
- 最终 Core bundle closure 3 项和 bridge transport parity 通过；覆盖复制后 bundled 校验、篡改/缺失依赖拒绝以及无 Git bundled 启动。
- 回归：Desktop 1067 tests / 1042 passed / 0 failed / 25 skipped；TypeScript、ESLint、Core live model、Desktop bridge manifest、transport parity 均通过。
- 未验证：没有在正式教师数据上代替教师点击接受；当前桌面自动化无法稳定完成原生鼠标点击，因此真实 UI 写入以隔离包 E2 和 API 重新读取证据为准。签名 updater、跨平台安装升级、系统通知点击和真实课堂内容质量仍未完成。

## 资源包实际启动 · 2026-09-09

- 本地desktop:prepare首次因追踪旧src-tauri/target及resources目录产生147条过长路径失败；排除旧构建、Git与开发状态后构建通过，阶段目录不含.git、.env.local、.edupi或旧src-tauri树。
- 资源包页面可读工作区，但启动Core失败，定位缺少运行校验脚本；Core `8cad4b4`补入声明资源，新增复制后真实启动回归由失败转通过。
- 整份资源包独立服务能启动Core并返回ready，XLSX和XLS名单预览分别HTTP200及1个工作表。未依赖开发目录来证明解析成功，未向名单写入预览数据。

## 安装版503诊断与原生通知 · 2026-09-09

- 安装版v0.3.6实际返回workspace 503。只读诊断定位一条手工任务关联的项目目录HTML被放入仅允许`.edupi/`的旧证据路径字段，导致整份快照失效。新投影保留通用产物索引及原文件，不将不兼容路径写入该旧字段；隔离回归从失败转为通过。
- 同时修正旧重试记录无逐步历史时的重复标准化：只兼容无Runtime绑定且与旧摘要完全一致的历史轮廓，新Runtime历史仍严格校验。真实数据只读调用handleSnapshotRequest返回ok及237条任务；未改写真实任务/产物。
- 原生提醒增加固定对象点击回调，macOS cargo check与原生参数测试通过，客户端任务/简报路由回归通过。Windows编译与安装版实际通知点击尚未验收；完全退出后旧通知的行为未验证。既有Rust最低版本未调整。

## 成长与设置文案 · 2026-09-09

- 教师成长只收录教学复盘与已确认成果，不再把一般后台文档/洞察报告当作教师成长；侧栏使用同一筛选计数。实际隔离页显示真实空态，未补模拟成果。
- 管理中心教学能力显示“已发布/已停用”等中文状态。当前Core仍禁止方法写入，UI保留只读状态，不声称自动演进完成。对应只读与文档分类渲染回归通过。
- 模型设置常用字段已通过中英文语言表显示；中文页实测服务名称、接口地址、模型ID、显示名称与推理/图片输入。高级密钥说明默认折叠，模型值及默认模型未变。

## 学生记录与观察数据库联动 · 2026-09-09

- 原观察数据库只读取C1观察、洞察和信号，遗漏班级模块学生事件；现从同一学生记录接口按学习/互动类别读取，合并展示原始记录及对话依据，侧栏计数同步。
- 隔离页面实测学情观察出现A班验收张三等式验算记录，班级运行出现同伴解释移项的互动记录。展开学习记录可读原始对话，不从多人同场推断好友。
- 从学生学习与互动编辑现有隔离记录，返回学情观察看到修订；随后通过同一界面恢复原文，历史保留。未修改真实学生数据。
- 分页按需要读取20条前缀并复用已读页，45条模拟接口回归覆盖跨页、缓存和读取失败。实际大记录集分页不由此替代；接口成功但空记录显示已连接，失败单独显示读取错误。

## 课程备注进入准备来源 · 2026-09-09

- Core `a9feb05`、Desktop `705fb70`，开发隔离目录。课程备注纳入任务摘要后，同一课次任务先显示计划中，旧产物不再作为当前候选。
- 页面立即准备首次返回model_error，显式重新准备后attempt2生成4份新文件。打开本节教案可见课程备注，展开图表格及三组相对面①③、②④、⑤⑥核对正确。
- 新教案 `artifact_e2c8240e0147f6ec6a1837438bb6f52a`；练习 `artifact_345e7fc85f80085cac512d160e460cba`。任务ID不变，旧正文第1至第5版文件仍保留。
- Core备注前后稳定任务ID/来源变化回归、生产G1组、配套闭包3项通过。实现跟踪 Core #39、Desktop #75；未更新安装包。

## 合并前回归 · 2026-09-09

- Desktop `npm test`：995 passed、24 skipped、0 failed；类型检查、lint通过。新正文修订及配套Core打包另有定向通过记录。
- Core默认完整测试在学生材料版本样例处失败：样例未带class_id，正确被精确范围过滤。补齐703班并新增未确定范围材料不参与断言；从失败处复跑 `test:students`、`test:closure-regressions`、`test:scoped-memory-runtime` 全通过，之前已经通过的默认测试段不重复执行。最终Core `ac63a95`。
- 本条不把跳过、未执行的安装版验证记为通过。R01–R17原账本继续保留未完成条件。

## 2026-09-09 受管早安简报

- 发现新Runtime只启动课前准备扫描，`runMorningBrief` 没有生产调用方。Core `182cd4e` 将已有简报函数接到受管启动及5分钟检查；每日去重、早6点之前不生成、失败Kernel记录沿用原实现。
- 生产Runtime测试实际启动即产生当日简报，重启后字节不变；原简报对象链接、安静时段测试通过。配套打包闭包3项通过。
- 在无既有简报的 `/private/tmp/edupi-onboarding.3BoidH` 启动开发服务并调用同安装端的ensure入口，自动生成 `daily/2026-09-09.md`。浏览器今天显示“今日已生成”“自动运行 已生成”，打开后日期正确，未录课程/校历保持真实空态。
- 旧验收目录已有当天简报时不覆盖，该观察不算新生成证据。真实休眠唤醒和安装版系统通知仍未验收。

## 2026-09-09 产物正文修订

- Core `f0ee560`，Desktop 当前工作树；沿原隔离数据测试，未更新安装版。
- 学生学案 `artifact_a4360b45abbdd28386b24daca151ff14` 在侧栏直接修改错误的“相对面是否贴合”为“所圈两面是否相对且不共用棱”，保存第2版；历史恢复第1版形成第3版，再恢复正确第2版形成第4版。旧文件与版本均保留。
- 任务入口与材料入口打开同一第4版。实际GLM会话 `01a08442-17a1-72e9-9e43-1233f7b447e6` 通过工具只修改第5条活动要求，保存第5版，其他正文与表格保持；重启后API与页面仍可读取该版本。
- 独立审查发现尾换行导致“已保存却报失败”，客户端已按Core尾换行规范提交；缺尾换行与已有多换行回归通过。还发现课表备注变更不拒绝旧正文修订，现绑定生成时真实来源摘要；新旧记录、合法审核、修改来源后的拒绝、保存失败恢复均有Core回归。
- 材料页面实际显示会议准备与课前准备的不同来源，课前文件通过带日期的课次ID关联到“数学 · 703”，不再全部显示空范围。
- 本记录证明该学案编辑与跨入口保存流程；不代表所有教学内容已审核，安装版仍待验收。

## 2026-09-09 空配置与引导续接

- Desktop `5241e7a` 加当前教程上一步修改、Core `00cbb93`，独立目录 `/private/tmp/edupi-onboarding.3BoidH`，开发服务端口 30142。空模型目录，不复用真实 API key。
- 管理中心新增自定义 URL，手填 `private-teacher-model`；测试服务故意不提供模型发现接口。连接测试显示 `已连接 · 102ms · HTTP 200 · OK`，保存并设为默认后聊天选择器显示该模型，实际发送收到“新手配置验收成功”。本地测试服务只验证配置与请求链路，不代表模型教学质量。
- 新手教程恢复到第二步时缺返回入口，已补“上一步”；浏览器实际返回模型设置，检查已保存默认模型后进入教师资料。手动保存“新手验收老师 / 数学 / 七年级 / 703”后进入第三步。
- 实际停止并重启开发服务，教师称呼、默认模型仍在，新手教程恢复第三步；再发一条新会话消息仍收到测试回复。安装版首启、完整首次备课和错误配置路径仍分别验收，不由该记录替代。
- 追加错误模型名验收：未保存的 `invalid-private-model` 测试显示 400 失败，改回正确名称后显示 `已连接 · 4ms · HTTP 200 · OK`。未覆盖无效 URL/key 的全部路径。结束后已停止该隔离服务与本地测试模型服务。

环境：macOS、本地 Next.js 开发版、独立数据与会话目录；不是已发布安装版。测试使用虚构学生，未改真实学生档案。模型使用已有 GLM-5.2，配置副本仅存本机。

## 提醒续聊

- 创建到期任务 `teacher-task-767144d7-8db5-4dc9-9f12-3196fb815024`，从提醒点击继续聊。
- 页面预填正确任务标题与输入位置；实际输入并发送简短测试消息。
- 模型回复“复测成功”；任务接口返回绑定会话 `01a07c97-48e0-706a-9098-c72f5a94d675`。
- 离开后从提醒返回相同会话 ID，原消息及回复可见。
- 实际停止并重启开发服务，页面重新读取后原会话和回复仍在。
- 首次验收发现会话创建后预填文字重新出现，已加一次性应用状态；复测发送后输入框为空、发送按钮禁用。
- 跨日发现未处理到期事项被撤下；改为截止日期不晚于今天时保留同一提醒身份，并补跨日回归。系统通知点击及安装版通知不在本次通过范围。

## 学生记录与网络

- 经名单导入接口创建“测试甲”“测试乙”，同属 703 班。
- 首次真实模型测试失败：会话未注册学生记录工具。配置 `/tmp` 与 Pi 会话 `/private/tmp` 的字符串比较造成误判。
- 修复为使用数据根目录真实路径；路径别名回归通过。
- 新会话 `01a07ca5-8614-758f-8e71-b7b3aa3a7ed6` 实际调用学生记录工具，持久化学习与互动各一条，来源指向该会话。
- 学生详情知识图谱显示学生、记录、知识点；页面缩放至 125%，复位至 100%，点击知识点显示关联记录。
- 页面修改学习记录，图中同步更新；展开历史看到旧内容。
- 页面删除学习记录后图中显示空态；人际网络仍保留两名参与者与一条互动事件，没有创建“好友”关系。
- 页面起始日期改为记录日期之后时为空，清除后恢复。
- 跨班同名和完整学生网络范围尚未验收完成，不能据此勾选 R12/R17 整项。

## 首次对话

- 暂存隔离历史会话，重启后以无会话环境打开 AI 对话。
- 复现没有项目选择导致加载/模型请求拒绝的问题。
- 自动读取配置工作区并复用目录验证接口登记后，页面显示工作区与 GLM-5.2。
- 实际发送“首次对话测试”，收到“就绪”，输入框清空。
- 这证明已配置模型、无历史会话的首次对话可用；从无 API 开始的完整新手教程仍需独立验收。

## 材料驱动备课与跨模块文件

- Core `f83a381`，Desktop 同次验收工作树；独立工作区提供数学 703 班课程及一份真实 Markdown 源文件。
- 实际运行 `desktop/preparation-worker.mjs`，得到四份文件；修改同一源文件，将橙色方格纸改为绿色，重新执行后仍是同一任务，旧 execution 标为 stale，新 execution 为 draft_ready。
- 第二次运行返回 `{"ok":true,"prepared":1,"skipped":0}`，模型会话 `01a07ccb-13ed-733d-9661-6aaeda895bff`。逐份读取四份输出，新教案与学案使用绿色方格纸、蓝色编号贴，材料清单正确说明系统已登记 2 人，不推断全班人数。
- 固定展开图为横排①②③④，⑤在②上方，⑥在②下方。独立按②为前面推导，①左、③右、④后、⑤上、⑥下；三组答案①③、②④、⑤⑥正确，新增判断题①②相对为错也正确。模型写出的“已逐题折叠验证”不作为证据，本次依据独立几何核对。
- 页面验收发现四个标题无文件按钮；修复统一索引后，教学产物页实际显示四份可点击文件。材料模块也显示相同四份，打开“本节教案”的路径与教学页一致，正文为新版本。旧版四份未混入最新列表。
- 预览默认隐藏 EduPi 内部元数据，标题显示“本节教案”；切换 Source 后元数据仍在。实际点击关闭按钮返回成功，1280×720 截图检查无溢出。
- 定向自动验证：`node scripts/test_requested_preparation.mjs`、`node scripts/test_generated_artifacts.mjs` 通过，覆盖四份文件、关联任务、可读路径、过期/移除后隐藏。`node --test scripts/preparation-materials.test.mjs scripts/model-output-repair.test.mjs` 六项通过，覆盖 Word 正文、范围、截断、删除档案人数与一次 JSON 修复。
- 这次用短篇固定题验证文件链路与答案，未证明所有学科内容质量；PDF/PPT 后续证据见下方，界面整套安装版未验收。

## 简报提醒与续聊

- 基于 Desktop `f2896fb` 后续改动，Core `f83a381`。在隔离工作区向 `runMorningBrief` 注入 `2026-09-08T08:00:00+08:00`，实际生成当天简报文件；不是等待真实早上调度。
- 复现投影把 UTC 文件修改时间当简报日期、ID 回退为数组下标的问题。每日简报改用文件名所属日期与日期标识，避免凌晨错日和列表重排串会话。
- 提醒中展开当天简报，点击查看事项，实际打开 `.edupi/output/daily/2026-09-08.md`。其余旧简报不作为今日新提醒。
- 点击继续聊，预填标题、简报摘录、来源文件及“我想补充”输入位置；实际发送后收到“简报续聊成功”。
- 绑定接口读取到 `document:daily:2026-09-08` → `01a07cdd-9942-71ec-9b84-e9f0ddfca733`。离开后经提醒返回同一会话；停止开发服务、重新启动、刷新后原消息与回复仍在。
- 重启后提醒接口仍只有一条简报提醒、绑定不变。系统通知及应用完全退出后的调度未验证。

## PPT 与 PDF 来源读取

- 生成两页真实 PPTX 隔离材料，课堂要求改为黄色方格纸；工作进程实际重新生成四份文件，返回 prepared=1，会话 `01a07ce8-d417-7178-9bbc-bf611d05e3ad`。教案、学案和清单均使用黄色方格纸，答案三组相对面仍正确。
- `node --test scripts/preparation-source-text.test.mjs scripts/preparation-materials.test.mjs` 五项通过，未跳过。PPT 调换实际 presentation 顺序后文本顺序正确，XML 字符正确解码；PDF 使用实际 PDF 字节，经本机 pdftotext 得到正文。
- Office 压缩目录限制抽出共享函数，原 Word 入口继续使用，原回归测试保留。没有新建第二套独立限制。
- 本机 PDF 文字提取通过，未打包 pdftotext 到 Windows；图片与图表未做视觉理解。材料清单明确说明只读了 PPT 两页文字。不能据此勾完 OCR 或安装版。
- XML 解析依赖固定到维护分支 0.8.15，依据 [上游安全修复记录](https://github.com/xmldom/xmldom/blob/master/CHANGELOG.md#0815)。新增独立目录的依赖复制/加载测试通过；这不替代安装包启动验收。

## 教学页审核联动

- 实际点击隔离备课任务的接受，复现 409：“该任务由 Today 待办审核管理”。教学页此前误用普通任务审核接口。
- 教学页按对象归属复用 Today 的同一审核请求，保留普通任务审核入口；不再提供该对象不支持的回滚与产物名称编辑。
- 复测 POST `/api/edupi/education` 返回 200，界面变为已接受，仅保留修改决定。刷新后历史显示接受时间和实际输入的审核意见。
- 从材料模块核对四份文件，均显示已确认；提醒接口中该任务的旧到期/准备完成提醒均为 withdrawn。
- 产物页收为四个真实文件按钮，去掉重复标题卡片；渲染测试覆盖文件仅出现一次、其他任务文件不混入、缺失文件禁用。

## 同名学生、转班与删除隔离

- Core 配套 `cb9cd73`；Desktop 学生身份改动与本记录同提交，仍为隔离开发环境。
- 导入接口创建 A班与 B班两名“验收张三”。页面分别显示班级，点击 B 的卡片后 URL 使用 B 的 ID。
- 页面手动保存 B 的特征，再读接口：A 特征仍为空，B 独立更新。学生对话入口预填 B 的 ID 与班级。
- 实际模型会话 `01a07d26-3597-7d0c-ac0f-552fd8c2c584` 先读取名单，再用 ID 写入 B 的学习记录和 A/B 共同互动。图中显示两个独立同名节点，互动为一条共同事件，没有生成好友边。
- 页面把 B 转入 C，ID 保持。旧学习记录保存 B班；同一会话后续新增记录保存 C班。A 另有一条独立学习记录，ID 与班级不变。
- 删除原生确认框显示了正确姓名和 C班，但自动化随后被弹窗锁住。这个页面确认操作未通过。已改为应用内确认框，取消/确定两条页面路径仍待浏览器恢复后验收。
- 单独经 DELETE 接口删除 C 的 ID，响应仅保留 A；再次读取 A 的学习记录仍为 1 条。原始 C 档案与事件保留为软删除，姓名总计数仍为 2，防止其旧姓名记忆转挂给 A。接口结果不替代页面验收。
- 新模型会话 `01a07d71-de9e-7364-a547-b573c1eff05c` 实际调用画像工具，结果为“学生已删除”；未恢复或新建档案。修复了模型把 ID 放进旧 student 参数时误报未建档的适配问题。
- Core 定向测试覆盖旧 key/ID 固定、同名不同班、来源重放、转班旧事件、删除 B 不影响 A、已删除 ID 拒写、画像与简报排除删除数据、A 已解决状态不抑制 B。已并入 `npm run test:students`。
- Desktop 渲染回归覆盖同名节点按 ID 分开、已删除同名人的裸姓名记忆不挂给剩余人、按 ID 删除结果校验；大班分页及安装版尚未验收。

## 后台 Word 与中文 OCR

- Desktop 基于 `9b8d81b` 的文档工具改动，Core `cb9cd73`；仍使用隔离开发环境。新增 `edupi_make_document`，实际生成标准 DOCX，不依赖 Python 或 Office；会话工具结果及历史恢复均登记文件。
- 后台任务 `agent_job_023fda940bf72cf8fa9d30b58c849ab9` 实际完成，产物 `f45e444b-97e9-481a-ab0c-3c00e2174787` 登记为 Word 教案。同一模型会话 `01a07d7e-530e-771f-9d07-3bc19544b69e` 修订并覆盖同一文件，再用 mammoth 读取正文。首次内容缺少展开图位置、后续出现错字，实际修订后对应问题消失；文件生成成功不等同内容一次正确。
- `node --test lib/edupi-document-tool.test.mjs` 4 项通过；真实 Core 环境下产物历史测试 4 项通过，无跳过。`npm test` 962 项中 944 通过、18 跳过、0 失败。跳过项不计入功能验收。
- 本机 Tesseract 的现有中文数据文件无法解析，第一次 OCR 结果错误，取消任务 `agent_job_795a9ba473d00fc0757ab1558104bc35` 后读取状态为 canceled，运行会话为空，没有登记产物。
- 从官方 tessdata_fast 获取中文数据到隔离目录，不改系统文件。重新经后台队列执行 `agent_job_4d0cccd0898c04605dbc274c950af5a2`，登记文本产物 `e30e6c90-bea9-438f-aa27-af011c3a4ff6`。读取文件与图片逐行一致：识别验收、周一 第2节 数学 703班、周二 第1节 数学 703班。
- OCR 依赖本机 Tesseract 与隔离修复的数据，尚未证明全新安装能直接识别。Word 可视排版、后台界面操作、安装版重启恢复仍待验证。

## 后台取消与重启

- 任务 `agent_job_91bc6ec34f19e82c7966de9b9c02775b` 执行到实际 `sleep 45` 工具调用时停止开发服务，重新启动后调用与桌面启动相同的 ensure 接口。原任务第二次执行，最终写入 recovery.txt，正文为恢复验收成功；无需手动重建任务。仅验证开发进程恢复，未验证安装版。
- 并行创建后立即取消 `agent_job_b5ce80c762c8b20bd1cd6d255a94ead4`，发现状态 canceled 但启动中的会话继续写出 canceled.txt。回归测试在修复前失败；修复为先持久化取消、会话启动后再次核对运行状态。
- 同样操作复测 `agent_job_0cabed2907d466c15bbc563cc02635f3`，状态 canceled、运行会话为空、取消文件不存在。未将最初失败测试算作通过。
- 并发测试同时暴露全局文件快照串归属：恢复任务误包含取消任务文件。后台完成收集限定当前任务目录，材料登记的并发归属仍在修复与复验，不能据原恢复测试声称产物隔离通过。
- 后续修复取代上述“修复中”：write/edit 按工具调用 ID 精确归属；原生文档使用完成事件返回路径；后台 bash 快照限定当前任务目录。双任务 `agent_job_a0eca567fddfb31b3edbf41082a6acba` 与 `agent_job_caa21e2281125fe50a76cba69ac2d946` 同时执行后各登记一份独立文件，重新读注册表会话分别为 `01a07d9d-0930-710e-aad3-6e2f62f1e185` 和 `01a07d9d-05ae-725d-bb83-c9a7f0084127`，正文分别属于甲、乙。普通聊天两个 bash 同时向共享目录写文件尚未覆盖。
- 文档、后台与真实 Core 产物定向测试合计 12 项通过，无跳过。页面仍受锁屏及先前原生确认框阻塞，未声称 UI 验收通过。

## 教学方法真实试用

- Core `66c839d` 修复教师输入标题不参与路由；独立备课此前完全未读取演进方法，现通过 Core 读取已发布且已批准的方法正文。只读方法，不伪造试用次数、课堂效果或路由记录。
- 隔离数据先冻结 `method-evaluation.json`，实际运行不加载方法的两份 baseline，再用 `try_teaching_method` 实际运行候选。首候选 `evo_mtrq9rsk_lkwh4k` 达到 3 次试用、两个会话；流程词评分训练 0.70→1.00，留出 0.60→1.00。
- 人工正文核对仍失败：训练输出误写“不相接，因此不是相对面”，留出输出混淆外边配对与 12 条棱。未晋级；经正式 review 接口拒绝，投影变为 retired，试用记录保留。确定性流程词评分不能证明数学正确或课堂效果。
- 修订候选 `evo_mtrqdl2o_1cqnuc` 补充相邻/相对面及外边配对事实，正在使用原冻结任务重新试用。原始输出和失败证据保留于隔离验收目录，未覆盖为成功结果。
- 后续结果取代“正在试用”：修订方法三次实际试用、两个会话完成，原冻结流程评分训练 0.70→1.00、留出 0.60→1.00。人工核对数学事实正确后，经接口提交明确标注的隔离测试审核，再通过模型 finalize、真实 reload，状态进入 promoted。未把测试审核冒充真实教师课堂反馈。
- 路由实测发现 `/tmp` 与 `/private/tmp` 别名造成 Core 目录比较及 Desktop 工作区门控失败。Core `9bd69d6` 比较真实目录，Desktop 三处门控复用路径规范化，保留历史 cwd。真实会话 `01a07efa-52b0-7320-b332-b159304439af`、`01a07efb-e372-70f5-88fd-9be51ecc8377` 路由记录 verified=true，无手改状态。
- 正式导入隔离课次 `method-reuse-acceptance`，同步候选后通过 preparation 接口运行，prepared=1。实际模型会话 `01a07efc-faf8-7833-85cc-7dbc2632b3e3` 生成四份文件；任务 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09` 的教案/学案写明方法 ID 和采用步骤1至4。
- 人工逐题核对练习：①③、②④、⑤⑥；②的邻面①③⑤⑥、对面④；平面不相接不等于相对，以①⑥为邻面反例；12−5=7对外边，均正确。仍是隔离材料内容核验，没有学生课堂效果证据。页面展示、后续方法更新触发重新备课尚未验收。

## 证据边界

记忆完整流程补验：真实会话 `01a07f9a-6fb5-7962-bb97-2f1072da8456` 调用 memory_write 记录偏好，页面教师偏好分类出现 `m_mts9gcpi_fexk`。手动修改到版本1、历史显示版本0、恢复旧版后版本2；新会话 `01a07fb0-66fb-7655-b0fc-bf98f2fd989f` 读到恢复后的正确内容。

页面删除后，新会话仍召回旧偏好，证明原实现只过滤投影。Core `f1e8d41` 修复召回、自动上下文、重复写入匹配及后台梦境/跨期分析；保存继续保留原始软删除条目和历史。完全重启服务后，新会话 `01a07fd4-d186-771b-bb5d-281b4cb8e54b` 实际 memory_recall 返回“没有找到相关记忆”，未恢复记录。新增默认回归覆盖这一失败链路和后台读取。

图片粘贴补验：使用隔离1像素PNG通过真实 Meta+V 粘贴，聊天输入出现附件和可用发送按钮；移除后发送重新禁用。测试后清理剪贴板恢复空文本。没有发送给不支持视觉的模型，不作为图片理解验收。移除图片按钮补充可访问名称。

Excel 名单补验：使用 `scripts/create-roster-acceptance-fixture.mjs` 生成明确测试名单，分别通过页面文件选择器上传 `.xlsx` 与 `.xls`，正确选中第二个“名单”工作表，预览1名学生和班级，再点击导入。后台重新读取分别为表格验收xlsx、表格验收xls，班级均为表格验收班，原名单保留。测试只写隔离目录，未使用真实教师名单。

本轮收尾检查：Desktop全测977项，957通过、20跳过、0失败；类型和lint通过。提供真实Core路径的手填并发、产物登记、方法注入定向15项中14通过、1旧fixture跳过。Core手填并发capture A→B测试证实A返回冲突且未批准B；对应Core版本 `67106a0`。跳过项目不计入完成。

成长来源文件补验：展开已发布方法，点击试用产物，在应用内实际读到对应四步复习流程；来源正文可读，关闭返回成长页面。方法详情默认隐藏YAML元数据，保留完整文件入口。

审核往返补验：从待确认看板接受后曾因选中任务被过滤而失去详情，已保留选中任务。修改决定时报400，Core `3ceef58` 修复已处理候选来源被移除的问题。实际页面接受→拒绝→接受全部成功，历史3条，最终已接受且保留修改决定；拒绝标签不再误报已完成，拒绝后重新审核入口不因隐藏产物而禁用。隔离任务仍为 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09`。

Word 页面补验：材料第二页打开 lesson.docx，再点预览，实际显示教学目标、课堂活动、指定展开图编号与修订后的绿色方格纸要求；关闭预览返回材料详情。仅验证应用内DOCX渲染，未用Office或安装版验收。

成长页面补验：教师专业成长随接受显示4份确认成果；能力成长显示实际已发布/已停用方法，展开可见方法正文与批准、试用、评估入口。文件来源按钮及每个折叠段仍待逐项点击，状态不作为课堂效果证明。

教师资料与教程补验：1280×720 下，教程第2步打开后缩为顶部条，手动填写按钮可点击。填写验收老师、数学、七年级、703，保存后页面和 onboarding API 均为已生效，检查继续进入第3步。新会话 `01a07f3f-5040-7f1e-863d-750e6d832f3e` 不调用工具直接正确回答四项资料，未要求重复介绍。已有 API 配置，未据此宣称零 API 首次连接通过。

大班分页补验：导入61名明确命名“分页测试01–61”的隔离名单，班级筛选后共3页，第二页25–48、第三页49–61；点击61打开其同名档案与对应ID。切换703只显示原2名学生，无空白末页；关闭档案后URL选中参数清除。主界面固定24/页，二级名单保留完整滚动访问。未向这些测试档案写学习或互动事实。

今天页简报摘要的 Markdown 标记与内部URL已清理，实际页面只显示内容名称，完整简报仍提供链接。

页面恢复后补验：课程链接 `calendarItem=method-reuse-acceptance&calendarKind=timetable&date=2026-09-09` 打开周三第3节数学703详情；抽屉内修改备注、保存后 API 重读及刷新均保留。已不存在的旧课次链接不会误选另一课次。

学生应用内删除确认补验：隔离档案 `student_e236169c-6c8e-4f12-a256-f3dd3fc532ff` 点击删除，取消后档案保留；再次确认删除，刷新后主名单与二级名单均从4变3，测试档案消失，选中URL清除，没有残留删除提示。原始记录为软删除留存。此结果取代此前页面确认受阻；不包含大班分页验收。

Core `eba2837` 追加方法版本联动：正文修订与撤回复用既有候选证据，任务ID不变、旧执行stale；真实存储/文件/节奏回归通过，模型使用确定性替身。该版本尚未通过真实模型修订重生成复验。

新增简报对象入口已通过隔离生成测试：课程、校历使用实际对象 ID 和日期；待确认、准备材料、失败任务使用实际任务 ID；产物文件检查存在后提供相对链接。真实执行候选生成产物后重新构建简报，文件链接可读取；重复运行不重复写入。Desktop 日期与对象解析测试通过，界面点击因锁屏未验收。这项记录不替代真实早上唤醒或通知送达。

上述通过项依据实际页面操作、模型回复和接口重新读取。单元测试及类型检查只作补充。未验证平台、未运行流程和外部账号依赖不计入完成；安装包按用户要求延后。

## 2026-09-09 新 Runtime 实测

环境仍为隔离数据的 macOS 开发服务30141，Core起点 `b6ed874`，Desktop当前工作树。安装版未替换。

- 私有模型 Host 使用隔离 agent 中已配置模型完成一道方程练习，输出 `2x+3=7 → 2x=4 → x=2`，代入检验正确。未写入真实教师数据。
- Runtime通过准备API启动成功。复制打包闭包测试3项全部通过，没有跳过；尚未进行安装包启动。
- 页面上传 Word `lesson.docx` 成功，但最初审核target ID被当作材料ID，导致缺少预览和摘录404。修正后，实际Word预览显示正方体展开图教案完整正文。
- 新上传携带已确认数学/703范围，页面展示正确。通过“用于备课”填写已核对摘录、确认后重新读取显示版本1，内容保留。
- 指定课前任务 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09` 点击立即准备，实际失败 `stale_source`。原因是持久计划仍引用旧无班级材料，新材料尚未同步进入证据。正在修复计划同步与精确范围选择，不能据上传成功认定完整备课通过。
- Markdown上传不在现有Word/PDF/图片范围；页面现在明确提示“不支持这类文件”，已实际重复选择验证，不再误报服务暂不可用。
- 单任务状态、显式重试、完成不重复生成已有针对性回归；跨模块新产物及重启继续待验收。

### 来源修复后

Core `df17267` 下，从实际任务页面点击立即准备，00:35:37入队，00:37:24生成完成，页面自动进入教学产物。四份新产物为本节教案、学生学案、练习与参考答案、材料准备清单。

任务 `teaching_before_class:timetable:method-reuse-acceptance:2026-09-09` 的执行修订为2、状态 `draft_ready`。新练习ID `artifact_ad2869e93856355bdabab80fea02d6bc`，从任务和材料模块分别实际打开，均为相同文件和正文。答案①③、②④、⑤⑥及②与④相对核对正确；学生学案没有提前给答案或虚构学生学情。教案/学案的空格展开图排版仍不够稳健，内容质量不整体勾完。

从材料返回关联任务，再次点击立即准备：执行修订仍为2、四份产物ID未变、历史仍7条，没有重复执行。安装版仍未替换；重启回读将在依赖漏洞修复后继续验证。

依赖修复及重启补验：Next与eslint-config-next更新16.3.4，sharp/js-yaml及兼容范围内的其余漏洞依赖定向更新。`npm audit --audit-level=high` 为0漏洞，types/lint与本机sharp PNG测试通过；`npm test` 1003项，979 passed、24 skipped、0 failed。跳过项目不计入安装验证。

实际停止并重启开发服务后，任务仍显示已准备及4份文件，学生学案从页面打开并显示原正文；API回读4个产物ID不变。隔离教师仍为验收老师/数学/703，指定工作区模型接口保留默认模型和5个模型。首次开发编译耗时27.7秒，不视为安装版启动性能。

### 受管聊天记忆

Core `f954af0`，Runtime已启动，实际页面使用已配置GLM-5.2：

- 会话 `01a083bd-93ad-744c-9a9b-aaaba47f4628` 通过 memory_write 保存“蓝色标题、单独参考答案”偏好，ID `m_a9e84dcc-c5c9-4570-817e-99901ba3b386`。教育记忆→教师偏好页面可见同一正文，API读回count1。
- 新会话 `01a083bf-a057-7ae7-8e51-544eebbea7d2` 调用查询工具，正确查回偏好。随后在该会话明确要求删除，受管删除后当前记忆匹配数为0。
- 再新建会话 `01a083c2-d50c-7a00-a6d3-987f9ae06f45` 查询，无蓝色标题记录且未恢复。只删除隔离测试记录，未修改真实教师数据。
- Core四类记忆写入、来源去重、70条长消息后snapshot可读、真实查询handler不写类别文件的回归通过。Class shadow不恢复写入。安装版以及此新记忆流程的重启实测仍待完成。

材料改版追加失败证据：通过页面把已确认摘录补充原文内容并保存v2，旧任务仍为draft_ready、执行修订2、旧四个产物ID保留在当前产物中。独立隔离复现还发现旧产物可被审核，且新事件复用了旧执行身份；当前正在修复立即失效、审核与重新生成。旧生成成功证据不替代材料改版后的验收。

### 后续重启与后台恢复

Core9af123d启动后，无需再保存摘录，旧任务已回planned、当前产物清空、审核按钮禁用。随后GLM的生成两次失败，未发布产物；只读诊断捕获模型输出是被单个JSON围栏包裹的合法四份内容。兼容修复已在独立树，待实际第三次重试。

重启后记忆查询会话 `01a083eb-a831-7512-912b-9b7c32ad380f` 实际查询并在页面显示无蓝色标题记录，未恢复。

后台任务由管理中心创建：普通交付 `agent_job_e08cb8aadddbd57cec216feb87ed1b8d` 完成，但在停止服务前已结束，因此不算重启证据。随后新建 `agent_job_5914fde0fadc626bfaee653f2d0bac0e`，在会话 `01a083f8-3a8b-7754-aa39-71fe797c30d7` 的 `sleep 120` 工具调用尚无结果时停止服务。重启并调用同一桌面启动入口ensure，任务自动被新owner领取，attempt2完成，没有重新提交或点击重试。产物 `c7471848-3b69-4df0-b087-c541b4d05314` 在材料页实际打开，正文为“重启后恢复成功”，运行列表最终为空。

恢复任务完成后残留历史error `lease_expired`，正在修正完成/重试状态清理及旧数据显示；不把这个文案问题隐藏为完全无风险。该证据是开发服务流程，不代表安装版恢复验收。
# 2026-09-09 计划合并与工作区完成联动

环境：macOS，30141 开发服务；数据为 `/private/tmp/edupi-acceptance.huWG5A/data` 隔离验收目录。Core `1630ebcbf358b673379f67cfa6ec2525e0b43ba7`，Desktop `codex/plan-overlaps-and-completion` 本轮工作树。未改正式教师数据和安装应用。

- 页面新建“待处理直达完成验收”，从待处理拖到已完成，页面显示成功并移列；刷新后保留。API 回读 boardStage=done、boardRevision=1，内容仍 not_generated/not_approved，没有伪造审核。任务ID `teacher-task-b46994bb-2c86-4616-9613-06320e6fadb9`。
- 会话推导为进行中的“提醒续聊复测”移回待处理，页面与Core接受首次人工覆盖。此前 stage_unchanged 的问题已修复。
- 使用真实配置模型完成隔离对话；会话 `01a08512-b81e-714d-8ee7-e69a88ec8151` 实际调用 edupi_update_task 查询并完成“提醒续聊验收”，工具回读 boardRevision=1，workspace API确认done。发现跨标签原有刷新未跟踪boardStage，继续补测，不能以API回读替代实时刷新验收。
- 通过生产 intake 导入两条不同名称的同日日历会议；真实模型判定日期重叠并提供理由，工作区可展开查看和选择保留项。点击合并后发现Core压缩备注空白导致回读比较失败，修正后从原intent点击“继续合并”成功。API只剩overlap-qa-a，包含双方备注及另一项标题；原始上传文件未删除。该失败也验证了部分写入恢复流程。
- 纯行为测试覆盖取消中断合并、来源变化、同班/跨班、分批检查、忽略/分别保留缓存；未将这些单元测试标成全部浏览器操作通过。
- 后续取代刷新欠项：同一真实模型会话调用reopen，工作区保持打开且没有手动刷新，既有监测将“提醒续聊验收”自动移回进行中。新增事件处理和监测签名16项测试通过；没有增加额外轮询。
- 实际停止并重启开发服务后回读：合并计划只剩a并保留全部备注；手动完成任务done/revision1；对话重开任务progress/revision2。随后移除隔离凭据副本，恢复真实30141服务，未覆盖安装应用。Core PR #40；Desktop提交见本记录所在PR。定向测试36通过、6跳过，刷新测试16通过，tsc/eslint通过；跳过项不计通过。

# 2026-09-12 最终配套运行时与桌面包复核

- 配套 Core 固定为 `deda34d7523b5267602a5629027c367a91acaa7a`，Desktop component manifest 为 `sha256:b29eb3ef9a9133de6d0d3c6528197d9bd13359ab4bd6ecf75c9d675e0845ddb7`。`npm run test:core-runtime-live-model` 全部通过，覆盖隔离 SDK、symlinked `node_modules` 权限、取消/超时、父进程退出回收、G1 产物读回、重放/重启、显式重试和来源失效。
- 最终 `EduPi.app` 由 `npm run desktop:prepare` 和 `tauri build --bundles app` 生成；包内服务重启后 `/api/edupi/status?summary=1` 返回 Core/projection/Kernel `ready`，工作区计数为 50 名学生、9 个课表、43 个校历节点、237 个任务，Core manifest hash 与 pin 一致，原生窗口可见。完整 build 的唯一失败步骤是 updater 私钥缺失，未生成签名更新包。
- `6258d9a` 后的冷启动序列记录为首次状态响应约 2.4 秒，首个观察到的 Core/projection 状态即为 `ready`；这条证据只覆盖当前 macOS 打包包，不替代其他平台和真实睡眠唤醒。
- 追加打包隔离文件 E2：临时数据根和临时 Pi 会话目录启动最终 `EduPi.app` 的 38471 实例；POST `/api/edupi/artifacts` 对历史 `write` toolResult 返回 `registered: 1, failedCount: 0`，GET 列表回读 1 份 `.edupi/output/package-e2.md`。关闭临时实例后正式 38472 包恢复 `Core/projection ready`，临时目录已清理。
- 最终包 loopback 模型配置复核：临时 localhost SSE mock 通过 `/api/models-config/test` 返回 `ok=true`、HTTP 200、`responseText=OK`，未写入持久模型配置，mock 服务已关闭。
- 同一最终 pin 的 Desktop 全量回归为 1067 tests、1042 passed、0 failed、25 skipped；`tsc --noEmit`、`npm run lint`、定向 C1/C2/C3 与 C6 recognition E2 均通过。C6 真实识别使用隔离材料，得到 3 个校历事件、1 个课表项，暂存目录回到 0。
- 仍未完成的证据边界：真实系统睡眠/唤醒与安装版自动补跑、系统通知点击、Windows/Linux 实机安装升级、签名/公证、零 API 首次完整备课、安装版后台恢复、真实课堂内容质量和外部连接器账号闭环。当前桌面自动化读取超时，原生页面点击未以接口结果替代。
