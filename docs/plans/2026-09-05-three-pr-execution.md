# 桌面端连续三阶段执行

本轮承接用户确认的三阶段。学生图谱为新增核心方向，不替代这条执行线。

## PR 1：日常交互闭环（进行中）

已实现：名单上传先预览，保留工作表及表头行，允许姓名/特征/家校备注列映射，十行分页，确认后通过既有 Core 导入接口写入；取消预览不写入。学生操作失败信息可折叠回看和清除。桌面控制长提示四秒后收成可点击的紧凑停止按钮。

验证：真实 703 XLSX 在隔离数据目录的浏览器中显示 50 人/5 页、翻页成功、确认后关闭预览；重复字段映射禁用导入并提示原因；无浏览器错误。新增预览测试覆盖标题行、多表、非标准表头、中文 CSV、超大工作表拒绝。全量 880 通过、16 跳过、0 失败；类型、Lint 通过。

剩余：日程、教学、材料、管理中心的查看/返回/编辑入口继续验收；当前记录不代表全站交互已经逐项验收。

## PR 2：课前自动准备（下一步）

接通 Desktop 常驻运行与 Core 课前执行，核对真实模型调用和材料落盘。下一节课应关联课程、重点、班级证据及产物；显示运行失败和重试。当前发现 `calendar_work_heartbeat` 要求调用方注入 runModel，已有飞书调用入口，Desktop 是否存在常驻调用需继续核实。不能以隔离测试的 mock 模型产物宣称真实自动备课可用。

## PR 3：运行状态与首次使用（待实施）

引导串起模型、教师资料、学生名单、课表和第一次备课；依据实际配置和运行结果显示完成状态。重启后配置、任务与结果一致；从通知可打开结果。Windows 原生验收单独记录。

每段单独提交与 PR，完成后更新本地安装包。下一入口是完成 PR 1 余下验收，再沿 PR 2 的 Core 执行调用链补齐缺口。
