# 对话任务与桌面备课执行

- `edupi_create_task` 与手动任务创建调用同一 Core 命令；保存来源教师消息，收到写入结果后才返回创建成功。
- 工作区分类、教学列表、教学侧栏计数共用 taskCategory；工具结束即刷新教育数据。
- Desktop 启动后开始每小时一次的未来两天准备检查；独立工作进程调用已固定 Core 的 calendar_work_heartbeat 和模型 runner。任务与产物仍由 Core 存储，Desktop 仅保留本进程运行状态。
- 教学页显示运行、结果或错误，提供立即检查/重试。父服务退出时工作进程退出；单轮五分钟超时，不与自身重叠。
- 真实模型隔离会话已调用创建工具；同一任务出现在教学页面，Core 重读后保留。
- 真实模型隔离后台运行产生一项校历准备与四份草稿，投影为 draft_ready。这个结果不等于所有课程已经自动备好。
- 定向测试、Core 任务写入 E2、全套 883 通过/16 跳过、类型/Lint 已通过。打包和最终安装在第三阶段后一起验证。

下一项：逐课执行验证、实际配置驱动的新手引导，最后打包安装。学生图谱不替代原三阶段。
